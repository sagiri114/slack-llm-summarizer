Metadata-Version: 2.4
Name: slack-llm-summarizer
Version: 0.1.0
Summary: Slack AI Agent MVP with summary, ask, and todo commands.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: aiohttp>=3.9.0
Requires-Dist: httpx>=0.27.0
Requires-Dist: python-dotenv>=1.0.1
Requires-Dist: slack-bolt>=1.21.0
Requires-Dist: slack-sdk>=3.33.0
Provides-Extra: dev
Requires-Dist: pytest>=8.0.0; extra == "dev"

# Slack AI Agent (slack-llm-summarizer)

基于 Slack **Socket Mode** 的频道 AI Agent。在频道内根据最近消息与 thread，提供结构化总结、问答与 TODO 提取，并强制引用可验证的 `source_id`（如 `M1`、`T2-R1`），由程序映射为 Slack permalink，降低幻觉风险。

## 功能

| 命令 | 说明 |
|------|------|
| `/summary [24h\|7d\|max:100\|provider:xxx]` | 总结重要更新、决定、日程、TODO、未解决问题 |
| `/ask <问题> [7d] [provider:xxx]` | 仅基于频道消息回答问题 |
| `/todo [7d] [provider:xxx]` | 提取任务、负责人、截止时间 |

也支持 mention：`@bot summary 24h`、`@bot ask ...`、`@bot todo 7d`。

## Quick Start（约 10 分钟）

### 1. Clone 并安装

```powershell
git clone <your-repo-url>
cd slack-llm-summarizer
python -m venv .venv
.\.venv\Scripts\Activate.ps1
make install
# 或: pip install -e ".[dev]"
# 或: .\scripts\setup.ps1
```

### 2. 创建 Slack App

1. 打开 [Slack API](https://api.slack.com/apps) → **Create New App** → **From manifest**
2. 粘贴仓库中的 `slack_app_manifest.yml`
3. **Socket Mode** → 开启
4. **Basic Information** → **App-Level Tokens** → Generate  
   - Scope: `connections:write`  
   - 得到 `xapp-...` → 写入 `.env` 的 `SLACK_APP_TOKEN`
5. **OAuth & Permissions** → **Install to Workspace**  
   - 得到 `xoxb-...` → 写入 `.env` 的 `SLACK_BOT_TOKEN`
6. 确认 **Slash Commands** 已有 `/summary`、`/ask`、`/todo`
7. 在目标频道邀请 bot：`/invite @lab-ai-agent`

### 3. 配置 `.env`

```powershell
Copy-Item .env.example .env
notepad .env
```

至少填写：

- `SLACK_BOT_TOKEN`
- `SLACK_APP_TOKEN`
- 所选 LLM 的 API Key（见下方 Provider 示例）

### 4. 测试 LLM 配置

```powershell
python -m slack_llm_summarizer.check_provider
# 或: make check
# 或: slack-llm-check
```

### 5. 启动 Bot

```powershell
make run
# 或: slack-llm-summarizer
# 或: .\scripts\dev.ps1
```

### 6. 在 Slack 测试

```text
/summary 24h
/ask 最近 demo 的进展是什么？
/todo 7d
/summary 24h provider:deepseek
```

## 一键命令

| 命令 | 作用 |
|------|------|
| `make install` | 安装依赖（editable） |
| `make setup` | 从 `.env.example` 复制 `.env`（Unix） |
| `make run` | 启动 bot |
| `make test` | 运行 pytest |
| `make check` | 测试当前 LLM 配置 |
| `.\scripts\setup.ps1` | Windows 安装 + 创建 `.env` |
| `.\scripts\dev.ps1` | Windows 启动 bot |

## LLM Provider 配置

通过 `.env` 中 `LLM_PROVIDER` 选择默认 provider，**无需改代码**。  
命令中可临时覆盖：`/summary 24h provider:gemini`。

| `LLM_PROVIDER` | 必填环境变量 |
|----------------|--------------|
| `openai` | `OPENAI_API_KEY`, `OPENAI_MODEL`, `OPENAI_BASE_URL` |
| `gemini` | `GEMINI_API_KEY`, `GEMINI_MODEL` |
| `deepseek` | `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`, `DEEPSEEK_BASE_URL` |
| `openai_compatible` | `OPENAI_COMPATIBLE_API_KEY`, `OPENAI_COMPATIBLE_MODEL`, `OPENAI_COMPATIBLE_BASE_URL` |

通用参数（所有 provider）：

- `LLM_TEMPERATURE`（默认 `0.2`）
- `LLM_MAX_TOKENS`（默认 `2000`）

### 示例：DeepSeek（推荐国内）

```env
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=你的key
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

### 示例：OpenAI

```env
LLM_PROVIDER=openai
OPENAI_API_KEY=你的key
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 示例：Gemini

```env
LLM_PROVIDER=gemini
GEMINI_API_KEY=你的key
GEMINI_MODEL=gemini-2.0-flash
```

### 示例：OpenAI 兼容网关（Ollama / vLLM / 其他）

```env
LLM_PROVIDER=openai_compatible
OPENAI_COMPATIBLE_API_KEY=你的key
OPENAI_COMPATIBLE_MODEL=你的模型名
OPENAI_COMPATIBLE_BASE_URL=https://your-gateway.example.com/v1
```

## 环境变量说明

完整列表见 `.env.example`。常用项：

| 变量 | 说明 |
|------|------|
| `SLACK_BOT_TOKEN` | Bot User OAuth Token（`xoxb-...`） |
| `SLACK_APP_TOKEN` | Socket Mode App-Level Token（`xapp-...`） |
| `LLM_PROVIDER` | 默认 LLM：`openai` / `gemini` / `deepseek` / `openai_compatible` |
| `ALLOWED_CHANNEL_IDS` | 可选，逗号分隔频道 ID，限制 bot 只在指定频道工作 |
| `SUMMARY_LANGUAGE` | 输出语言：`zh` / `en` / `ja` |
| `MAX_CONTEXT_CHARS` | 送入 LLM 的最大上下文字符数 |

## 项目结构

```text
src/slack_llm_summarizer/
  app.py              # Slack Bolt 路由
  config.py           # 环境变量与 provider 配置
  startup.py          # 启动前友好校验
  commands.py         # /summary /ask /todo
  context.py          # 消息清洗与 source_id
  retrieval.py        # /ask 关键词检索
  prompts.py          # LLM 提示词
  formatter.py        # Slack 输出与错误文案
  validator.py        # citation 校验
  check_provider.py   # LLM 配置诊断 CLI
  providers/          # OpenAI / Gemini / DeepSeek / compatible
```

## 安全说明

- **不要**将 Slack Token、API Key 写入代码、README 或提交到 Git
- `.env` 已在 `.gitignore` 中，仅提交 `.env.example`（空值）
- 启动日志**不会**打印 secret
- 不持久化 Slack 原文；仅在内存中用于当次请求
- 建议使用 `ALLOWED_CHANNEL_IDS` 限制生产频道

## 常见问题

### 启动时报缺少 `SLACK_BOT_TOKEN`

在 Slack App → **OAuth & Permissions** 安装 workspace 后，复制 Bot User OAuth Token 到 `.env`。

### 启动时报缺少 `SLACK_APP_TOKEN`

开启 **Socket Mode**，创建 App-Level Token（scope: `connections:write`）。

### Bot 回复「处理失败」且与 LLM 相关

1. 确认 `LLM_PROVIDER` 与对应 `*_API_KEY` 一致（例如用 DeepSeek 时设为 `deepseek`）
2. 运行 `python -m slack_llm_summarizer.check_provider` 查看具体错误
3. 检查 API 余额、模型名、`base_url` 是否正确

### `/summary` 显示「没有可分析的消息」

- bot 是否已 `/invite` 进该频道
- 时间范围内是否确有消息（可调大：`/summary 7d`）

### `ModuleNotFoundError: aiohttp`

```powershell
pip install -e .
```

### 想换 provider 不想改 `.env`

```text
/summary 24h provider:deepseek
/ask provider:gemini 谁负责 demo？
```

## 开发与测试

```powershell
make test
slack-summary-dry-run --input samples/messages.json --provider deepseek
```

## 参考文档

- [Slack Socket Mode](https://docs.slack.dev/apis/events-api/using-socket-mode/)
- [Slack conversations.history](https://docs.slack.dev/reference/methods/conversations.history/)
- [DeepSeek API](https://api-docs.deepseek.com/)
